$('.feature-items').slick({
    infinite: true,
    slidesToShow: 4,
    slidesToScroll: 1,
    prevArrow: prevArrow,
    nextArrow: nextArrow,
    customPaging: true
});
$('.feature-items-2').slick({
    infinite: true,
    slidesToShow: 4,
    slidesToScroll: 1,
    prevArrow: prevArrow2,
    nextArrow: nextArrow2,
    customPaging: true
});